---
page_title: "{{.Name}} {{.Type}} - {{.RenderedProviderName}}"
subcategory: "Hosts and Templates"
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

## Template groups

`groups` holds **template** group ids from
[`zabbix_templategroup`](templategroup).

## Version-gated attributes

| Attribute | Requires |
|---|---|
| `vendor_name`, `vendor_version` | must be set together |
| `readme`, `wizard_ready` | Zabbix 7.4 or newer |

Servers below Zabbix 7.4 are rejected during provider configuration.

## Example Usage

{{ tffile .ExampleFile }}

{{ .SchemaMarkdown | trimspace }}

## Import

{{ codefile "shell" .ImportFile }}
